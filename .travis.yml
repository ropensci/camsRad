# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
#sudo: false
cache: packages

addons:
   apt:
     packages:
       - netcdf-bin # needed for netcdf4 package

notifications:
  email:
    on_success: change
    on_failure: change
